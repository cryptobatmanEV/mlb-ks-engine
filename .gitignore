.env
__pycache__/
*.pyc
*.log

# Raw/processed data - large, regenerated by ingestion & feature scripts
data/raw/
data/processed/**
# park_factors_k.csv is static reference data committed to git (not regenerated daily)
!data/processed/park_factors_k.csv

# Intermediate prediction CSVs — within-job use only (fair_odds reads them
# in the same run). Not committed; log_results reads data/outputs/ instead.
data/predictions/

# Trained models - ks_model.pkl is tracked (needed by the daily pipeline in CI)
# and refreshed by weekly_retrain.yml.

# Web app build artifacts
.DS_Store
web/.env.local
web/node_modules/
web/.next/
web/tsconfig.tsbuildinfo
